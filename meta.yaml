# ===================================================
# meta infos
# ===================================================

meta:
  project: ci.os.lmp
  include:
    - version.yaml
    - tools/curl/dobi.yaml
    - mender-on-verdin-tdx-dunfell/dobi.yaml

# ===================================================
# alias
# ===================================================

alias=version:
  tasks:
    - buildingblock-version

alias=build-all:
  tasks:
    - mender-on-verdin-tdx-dunfell-build-minimal-image
  annotations:
    description: "[alias] build all images"

