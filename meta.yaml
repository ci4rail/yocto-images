# ===================================================
# meta infos
# ===================================================

meta:
  project: yocto-images
  include:
    - version.yaml
    - dobi.yaml
    - cpu01-base-image/dobi.yaml
    - edgefarm-image/dobi.yaml
    - devtools-image/dobi.yaml
    - edgefarm-devtools-image/dobi.yaml

# ===================================================
# alias
# ===================================================

alias=version:
  tasks:
    - buildingblock-version
