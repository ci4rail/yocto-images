# ===================================================
# meta infos
# ===================================================

meta:
  project: ci.os.lmp
  include:
    - version.yaml
    - dobi.yaml
    - tools/curl/dobi.yaml
    - yocto/dobi.yaml

# ===================================================
# alias
# ===================================================

alias=version:
  tasks:
    - buildingblock-version

alias=build-all:
  tasks:
    - cpu01-bringup-build-image
    - cpu01-edgefarm-build-image
    - verdindev-edgefarm-build-image
  annotations:
    description: "[alias] build all images"

